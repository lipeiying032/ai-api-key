{
  "name": "llm-gateway",
  "version": "3.1.0",
  "private": true,
  "description": "Unified LLM Gateway - Cloudflare Worker",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "wrangler": "^3.99.0"
  }
}